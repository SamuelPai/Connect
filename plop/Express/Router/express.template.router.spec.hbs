const expect = require('chai').expect;
const {{pascalCase name}} = require('./{{pascalCase name}}.router');

describe('Router - {{pascalCase name}}', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('POST - /{{pascalCase name}}', () => {
    it('should create a single {{pascalCase name}}', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /{{pascalCase name}}', () => {
    it('should return an array of all {{pascalCase name}}', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /{{pascalCase name}}/:id', () => {
    it('should return a single {{pascalCase name}} with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('PUT - /{{pascalCase name}}/:id', () => {
    it('should update a single {{pascalCase name}} with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('DELETE - /{{pascalCase name}}/:id', () => {
    it('should delete a single {{pascalCase name}} with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});

const express = require('express');
const router = express.Router();
const controller = require('../../controllers/{{pascalCase name}}/{{pascalCase name}}.controller');

// MIDDLEWARE - Router Specific
router.use(function timeLog (req, res, next) {
  console.log('Time: ', Date.now());
  next();
});

// ROUTES
router.post('/{{pascalCase name}}', controller.create);
router.get('/{{pascalCase name}}', controller.getAll);
router.get('/{{pascalCase name}}/:id', controller.getById);
router.put('/{{pascalCase name}}/:id', controller.updateById);
router.delete('/{{pascalCase name}}/:id', controller.updateById);

module.exports = router;

const expect = require('chai').expect;
const Activity = require('./Activity.router');

describe('Router - Activity', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('POST - /Activity', () => {
    it('should create a single Activity', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /Activity', () => {
    it('should return an array of all Activity', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /Activity/:id', () => {
    it('should return a single Activity with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('PUT - /Activity/:id', () => {
    it('should update a single Activity with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('DELETE - /Activity/:id', () => {
    it('should delete a single Activity with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});

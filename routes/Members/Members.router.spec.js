const expect = require('chai').expect;
const Members = require('./Members.router');

describe('Router - Members', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('POST - /Members', () => {
    it('should create a single Members', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /Members', () => {
    it('should return an array of all Members', () => {
      expect(true).to.equal(false);
    });
  });

  describe('GET - /Members/:id', () => {
    it('should return a single Members with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('PUT - /Members/:id', () => {
    it('should update a single Members with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('DELETE - /Members/:id', () => {
    it('should delete a single Members with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});

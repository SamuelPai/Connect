import React from 'react';
import { AddActivityProps } from './AddActivity.propTypes';

export const AddActivity = props => {
    return (
        <div>
            Add Activity component atom
        </div>
    );
};

AddActivity.propTypes = AddActivityProps.propTypes;
AddActivity.defaultProps = AddActivityProps.defaultProps;

const expect = require('chai').expect;
const Trips = require('./Trips.controller');

describe('Controller - Trips', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('create', () => {
    it('should create a single Trips', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getById', () => {
    it('should return a single Trips with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getAll', () => {
    it('should return an array of all Trips', () => {
      expect(true).to.equal(false);
    });
  });

  describe('updateById', () => {
    it('should update a single Trips with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('deleteById', () => {
    it('should delete a single Trips with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});
